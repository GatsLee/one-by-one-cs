---
title: "OSI 7계층과 TCP/IP"
topic: "03-network"
order: 02
---


## 핵심 개념

### 왜 계층 모델이 필요한가?

네트워크 통신은 매우 복잡하다. 물리적 전기 신호부터 웹 페이지 렌더링까지 수많은 과정이 필요하다. 이것을 **계층(Layer)**으로 나누면:

- 각 계층이 독립적으로 발전 가능 (하드웨어 변경이 소프트웨어에 영향 없음)
- 문제 발생 시 어느 계층의 문제인지 빠르게 파악 가능
- 표준화 용이

> **비유**: 우편 시스템도 계층적이다. 편지 작성(응용) → 봉투에 넣기(표현) → 우체국 접수(세션) → 배송 경로 결정(네트워크) → 트럭으로 운반(전송/데이터링크) → 도로(물리)

### OSI 7계층 모델

ISO(국제 표준화 기구)에서 정의한 통신 표준 모델. **이론적 참조 모델**.

<Mermaid chart="
graph TD
    L7[&quot;7. 응용 계층 (Application)\nHTTP, FTP, SMTP, DNS\n← 사용자와 직접 상호작용&quot;]
    L6[&quot;6. 표현 계층 (Presentation)\n암호화, 압축, 인코딩 (SSL/TLS, JPEG)\n← 데이터 형식 변환&quot;]
    L5[&quot;5. 세션 계층 (Session)\n세션 설정/유지/종료 (NetBIOS, RPC)\n← 연결 관리&quot;]
    L4[&quot;4. 전송 계층 (Transport)\nTCP, UDP / 포트 번호\n← 종단 간 통신&quot;]
    L3[&quot;3. 네트워크 계층 (Network)\nIP, ICMP, ARP / IP 주소\n← 라우팅, 경로 결정&quot;]
    L2[&quot;2. 데이터 링크 계층 (Data Link)\n이더넷, Wi-Fi / MAC 주소\n← 인접 노드 간 전송&quot;]
    L1[&quot;1. 물리 계층 (Physical)\n전기 신호, 광 신호, 케이블, 허브\n← 비트 전송&quot;]
    L7 --> L6 --> L5 --> L4 --> L3 --> L2 --> L1
" />

### 각 계층 상세 설명

#### 7계층 — 응용 계층 (Application Layer)

사용자가 직접 사용하는 응용 프로그램과 네트워크 사이의 인터페이스.

| 프로토콜 | 용도 | 포트 |
|---------|------|------|
| HTTP/HTTPS | 웹 페이지 전송 | 80/443 |
| FTP | 파일 전송 | 20, 21 |
| SMTP | 이메일 발송 | 25, 587 |
| DNS | 도메인 → IP 변환 | 53 |
| SSH | 원격 접속 (암호화) | 22 |

#### 6계층 — 표현 계층 (Presentation Layer)

데이터의 **형식 변환, 암호화, 압축**을 담당.

- 데이터 인코딩: ASCII, UTF-8, EBCDIC 변환
- 암호화/복호화: SSL/TLS (실질적으로 4~7계층에 걸침)
- 압축: 데이터 크기 줄이기

#### 5계층 — 세션 계층 (Session Layer)

통신 **세션의 설정, 유지, 종료**를 관리.

- 대화 제어: 반이중(Half-duplex), 전이중(Full-duplex)
- 동기화: 체크포인트 설정으로 오류 시 복구 지점 제공
- 실제로는 응용 계층에 통합되는 경우가 많음

#### 4계층 — 전송 계층 (Transport Layer)

**종단 간(End-to-End)** 신뢰성 있는 데이터 전송. 포트 번호로 프로세스 식별.

<Mermaid chart="
graph LR
    A[&quot;프로세스 A\n(브라우저)\n포트 12345&quot;] -- &quot;TCP 연결 (신뢰성)&quot; --> B[&quot;프로세스 B\n(웹서버)\n포트 80&quot;]
    subgraph 송신호스트[송신 호스트]
        A
    end
    subgraph 수신호스트[수신 호스트]
        B
    end
" />

- **TCP**: 연결 지향, 신뢰성, 순서 보장, 흐름/혼잡 제어
- **UDP**: 비연결, 빠름, 순서/신뢰성 보장 안 됨

#### 3계층 — 네트워크 계층 (Network Layer)

**라우팅**: 패킷이 출발지에서 목적지까지 최적 경로를 따라 전달.

- **IP 주소**: 네트워크상 장치 식별 (논리적 주소)
- **라우터**: 경로 결정 장비
- **IP 프로토콜**: 패킷 전달 (비연결, 최선형 전달)
- **ICMP**: 에러 보고 (ping 명령어)
- **ARP**: IP 주소 → MAC 주소 변환

#### 2계층 — 데이터 링크 계층 (Data Link Layer)

**인접한 노드 간**의 데이터 전송. 프레임 단위.

- **MAC 주소**: 하드웨어 고유 식별자 (48비트, AA:BB:CC:DD:EE:FF)
- **프레임**: 데이터 링크 계층의 PDU (Protocol Data Unit)
- **이더넷**: 유선 LAN 표준
- **Wi-Fi (802.11)**: 무선 LAN 표준
- **스위치**: 이 계층에서 동작하는 장비

#### 1계층 — 물리 계층 (Physical Layer)

**비트를 물리적 신호로 변환**하여 전송.

- 전기 신호 (구리선), 광 신호 (광섬유), 전파 (무선)
- 케이블 종류, 커넥터, 핀 배치
- 허브, 리피터: 신호 증폭/재생

### TCP/IP 4계층 모델

실제 인터넷에서 사용하는 모델. OSI 7계층보다 **실용적**.

<Mermaid chart="
graph LR
    subgraph OSI[&quot;OSI 7계층&quot;]
        direction TB
        O7[&quot;7. 응용&quot;] --> O6[&quot;6. 표현&quot;] --> O5[&quot;5. 세션&quot;] --> O4[&quot;4. 전송&quot;] --> O3[&quot;3. 네트워크&quot;] --> O2[&quot;2. 데이터링크&quot;] --> O1[&quot;1. 물리&quot;]
    end
    subgraph TCPIP[&quot;TCP/IP 4계층&quot;]
        direction TB
        T4[&quot;4. 응용\nHTTP, DNS, FTP, SMTP&quot;] --> T3[&quot;3. 전송\nTCP, UDP&quot;] --> T2[&quot;2. 인터넷\nIP, ICMP, ARP&quot;] --> T1[&quot;1. 네트워크 인터페이스\n이더넷, Wi-Fi&quot;]
    end
    O7 -.-> T4
    O6 -.-> T4
    O5 -.-> T4
    O4 -.-> T3
    O3 -.-> T2
    O2 -.-> T1
    O1 -.-> T1
" />

**TCP/IP 모델의 특징:**
- OSI의 5, 6, 7계층을 **응용 계층** 하나로 통합
- OSI의 1, 2계층을 **네트워크 인터페이스 계층**으로 통합
- 실제 인터넷 프로토콜에 기반한 실용적 모델

### 패킷 캡슐화 / 역캡슐화

데이터가 각 계층을 지나면서 **헤더(Header)**가 추가(캡슐화)되고, 수신 측에서 역순으로 제거(역캡슐화)된다.

<Mermaid chart="
graph TD
    subgraph 송신측[&quot;송신 측 — 캡슐화&quot;]
        direction TB
        E1[&quot;응용 계층: 데이터&quot;]
        E2[&quot;전송 계층: TCP 헤더 | 데이터 (세그먼트)&quot;]
        E3[&quot;네트워크 계층: IP 헤더 | TCP 헤더 | 데이터 (패킷)&quot;]
        E4[&quot;데이터 링크: ETH 헤더 | IP | TCP | 데이터 | FCS (프레임)&quot;]
        E5[&quot;물리 계층: 01001101001011... (비트)&quot;]
        E1 --> E2 --> E3 --> E4 --> E5
    end
    subgraph 수신측[&quot;수신 측 — 역캡슐화&quot;]
        direction BT
        D5[&quot;물리 계층: 비트 수신&quot;]
        D4[&quot;데이터 링크: ETH 헤더 제거&quot;]
        D3[&quot;네트워크 계층: IP 헤더 제거&quot;]
        D2[&quot;전송 계층: TCP 헤더 제거&quot;]
        D1[&quot;응용 계층: 데이터 획득&quot;]
        D5 --> D4 --> D3 --> D2 --> D1
    end
    E5 -- &quot;네트워크 전송&quot; --> D5
" />

**각 계층의 PDU (Protocol Data Unit):**

| 계층 | PDU 이름 | 내용 |
|------|---------|------|
| 응용 | 메시지/데이터 | 순수 애플리케이션 데이터 |
| 전송 | **세그먼트** (TCP) / 데이터그램 (UDP) | + 포트 번호, 시퀀스 번호 |
| 네트워크 | **패킷** | + 출발지/목적지 IP 주소 |
| 데이터 링크 | **프레임** | + 출발지/목적지 MAC 주소 |
| 물리 | **비트** | 0과 1의 전기/광 신호 |

### 실제 네트워크 장비와 계층

<Mermaid chart="
graph TD
    L74[&quot;7-4 계층\n방화벽, 로드밸런서, L4/L7 스위치&quot;]
    L3[&quot;3 계층\n라우터 (IP 주소 기반 경로 결정)&quot;]
    L2[&quot;2 계층\n스위치 (MAC 주소 기반 프레임 전달)&quot;]
    L1[&quot;1 계층\n허브, 리피터 (신호 증폭/재생)&quot;]
    L74 --> L3 --> L2 --> L1
" />

| 장비 | 계층 | 역할 |
|------|------|------|
| **허브** | 1 | 신호를 모든 포트로 전달 (브로드캐스트) |
| **스위치** | 2 | MAC 주소 테이블로 해당 포트로만 전달 |
| **라우터** | 3 | IP 주소 기반 패킷 경로 결정 |
| **방화벽** | 3-7 | 패킷 필터링, 접근 제어 |
| **로드밸런서** | 4-7 | 트래픽 분산 |

### 실제 웹 요청 흐름 예시

브라우저에서 `https://www.example.com`에 접속할 때:

<Mermaid chart="
sequenceDiagram
    participant B as 브라우저
    participant S as 서버 (93.184.216.34)
    B->>S: 1. DNS 조회: www.example.com → 93.184.216.34
    B->>S: 2. TCP 3-way handshake (4계층)
    B->>S: 3. TLS 핸드셰이크 (암호화 설정)
    B->>S: 4. HTTP GET /index.html (7계층)
    Note over B: 캡슐화 과정
    Note over B: 응용: HTTP GET /index.html
    Note over B: 전송: TCP HDR(포트 443) | HTTP
    Note over B: 네트워크: IP HDR | TCP | HTTP
    Note over B: 데이터 링크: ETH HDR | IP | TCP | HTTP | FCS
    Note over B: 물리: 비트 스트림
    S-->>B: 라우터들을 거쳐 목적지 서버에 도달
    Note over S: 역캡슐화 후 HTTP 응답 생성
    S-->>B: HTTP 응답 반환 (동일한 캡슐화 과정)
" />
## 정리

| 항목 | 내용 |
|------|------|
| OSI 7계층 | 이론적 참조 모델 (물리→데이터링크→네트워크→전송→세션→표현→응용) |
| TCP/IP 4계층 | 실용적 모델 (네트워크 인터페이스→인터넷→전송→응용) |
| 캡슐화 | 데이터가 하위 계층으로 내려가면서 헤더가 추가되는 과정 |
| 역캡슐화 | 수신 측에서 헤더를 역순으로 제거하는 과정 |
| PDU | 세그먼트(전송), 패킷(네트워크), 프레임(데이터링크), 비트(물리) |
| 주소 체계 | MAC 주소(2계층), IP 주소(3계층), 포트 번호(4계층) |
| 장비 | 허브(1계층), 스위치(2계층), 라우터(3계층) |
