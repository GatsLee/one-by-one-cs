---
title: "프로세스와 스레드"
topic: "02-os"
order: 01
---


## 핵심 개념

### 프로그램 vs 프로세스

| 구분 | 프로그램 | 프로세스 |
|------|---------|---------|
| 정의 | 디스크에 저장된 실행 파일 | 메모리에 적재되어 실행 중인 프로그램 |
| 상태 | 정적 (파일) | 동적 (실행 중) |
| 비유 | 요리 레시피 (종이에 적힌 것) | 실제로 요리하는 행위 |

> **비유**: 프로그램은 "요리 레시피"이고, 프로세스는 "레시피대로 실제로 요리하는 것"이다. 같은 레시피(프로그램)로 여러 번 요리(프로세스)를 할 수 있다.

### 프로세스의 메모리 구조

프로세스는 메모리에서 4개의 영역을 할당받는다.

<Mermaid chart="graph TD
  Stack[&quot;스택(Stack)
지역 변수, 함수 호출 정보
↓ 아래로 성장&quot;]
  Free[&quot;빈 공간&quot;]
  Heap[&quot;힙(Heap)
동적 할당 메모리 (malloc, new)
↑ 위로 성장&quot;]
  Data[&quot;데이터(Data)
전역 변수, 정적 변수&quot;]
  Code[&quot;코드(Code/Text)
실행할 기계어 명령어 (읽기 전용)&quot;]

  Stack --> Free --> Heap --> Data --> Code

  style Stack fill:#ff9999,stroke:#333
  style Heap fill:#99ccff,stroke:#333
  style Data fill:#99ff99,stroke:#333
  style Code fill:#ffcc99,stroke:#333
  style Free fill:#f5f5f5,stroke:#999,stroke-dasharray: 5 5
" />

| 영역 | 저장 내용 | 특징 |
|------|----------|------|
| **코드(Text)** | 실행할 기계어 코드 | 읽기 전용, 공유 가능 |
| **데이터(Data)** | 전역 변수, 정적 변수 | 초기화된 데이터 + 미초기화 데이터(BSS) |
| **힙(Heap)** | 동적 할당 메모리 | 위로 성장, 프로그래머가 관리 |
| **스택(Stack)** | 지역 변수, 매개변수, 복귀 주소 | 아래로 성장, 자동 관리 (LIFO) |

### 프로세스 상태 전이

프로세스는 생성부터 종료까지 여러 상태를 거친다.

<Mermaid chart="graph TD
  Start(( )) -->|생성| New([New])
  New -->|메모리 할당, PCB 생성| Ready([Ready])
  Ready -->|CPU 할당 - 디스패치| Running([Running])
  Running -->|타임아웃 / 인터럽트| Ready
  Running -->|I/O 요청 / 이벤트 대기| Waiting([Waiting])
  Waiting -->|I/O 완료| Ready
  Running -->|종료| Terminated([Terminated])

  style Start fill:#333,stroke:#333
  style New fill:#a8d8ea,stroke:#333
  style Ready fill:#99ff99,stroke:#333
  style Running fill:#ff9999,stroke:#333
  style Waiting fill:#ffcc99,stroke:#333
  style Terminated fill:#ccc,stroke:#333
" />

| 상태 | 설명 |
|------|------|
| **New** | 프로세스가 생성되는 중 |
| **Ready** | CPU를 할당받을 준비가 됨 (준비 큐에서 대기) |
| **Running** | CPU에서 실행 중 (단일 코어에서는 동시에 1개만) |
| **Waiting (Blocked)** | I/O나 이벤트 완료를 기다리는 중 |
| **Terminated** | 실행이 끝남, 자원 반납 중 |

**주요 전이:**
- Ready → Running: **디스패치(Dispatch)** — 스케줄러가 CPU 할당
- Running → Ready: **타임아웃/선점** — 시간 할당량 초과 또는 우선순위 높은 프로세스 등장
- Running → Waiting: **I/O 요청** — 디스크 읽기 등 대기 필요
- Waiting → Ready: **I/O 완료** — 다시 CPU 대기

### PCB (Process Control Block)

운영체제가 각 프로세스를 관리하기 위한 **정보 블록**. 프로세스 1개당 PCB 1개.

| PCB 필드 | 저장 내용 |
|---------|----------|
| **프로세스 ID (PID)** | 프로세스 고유 식별 번호 |
| **프로세스 상태** | Ready, Running, Waiting 등 |
| **프로그램 카운터 (PC)** | 다음 실행할 명령어 주소 |
| **CPU 레지스터 값** | 문맥 교환 시 저장/복원 |
| **CPU 스케줄링 정보** | 우선순위, 스케줄링 큐 포인터 |
| **메모리 관리 정보** | 페이지 테이블, 세그먼트 테이블 |
| **I/O 상태 정보** | 열린 파일 목록 |
| **계정 정보** | CPU 사용 시간 등 |

### 컨텍스트 스위칭 (Context Switching)

CPU가 현재 실행 중인 프로세스를 다른 프로세스로 교체하는 과정.

<Mermaid chart="graph TD
  A[&quot;프로세스 A 실행 중&quot;] -->|인터럽트 / 시스템콜| Save[&quot;OS: A의 상태를 PCB_A에 저장
(레지스터, PC, 스택 포인터 등)&quot;]
  Save --> Sched[&quot;OS: 스케줄러가 B를 선택&quot;]
  Sched --> Restore[&quot;OS: PCB_B에서 B의 상태 복원
(레지스터, PC, 스택 포인터 등)&quot;]
  Restore --> B[&quot;프로세스 B 실행 시작&quot;]

  style A fill:#99ccff,stroke:#333
  style Save fill:#ffcc99,stroke:#333
  style Sched fill:#ffcc99,stroke:#333
  style Restore fill:#ffcc99,stroke:#333
  style B fill:#99ff99,stroke:#333
" />

**컨텍스트 스위칭 비용:**
- 레지스터 저장/복원
- 메모리 캐시 무효화 (캐시 미스 증가)
- TLB(주소 변환 캐시) 갱신
- 파이프라인 플러시

> 컨텍스트 스위칭은 순수 오버헤드다. 이 동안 유용한 작업이 수행되지 않는다. 따라서 너무 잦은 컨텍스트 스위칭은 성능을 저하시킨다.

### 스레드 (Thread)

스레드는 **프로세스 내의 실행 단위**. 하나의 프로세스 안에 여러 스레드가 존재할 수 있다.

<Mermaid chart="graph LR
  subgraph Single[&quot;단일 스레드 프로세스&quot;]
    S_Code[&quot;코드&quot;]
    S_Data[&quot;데이터&quot;]
    S_Heap[&quot;힙&quot;]
    S_Stack[&quot;스택&quot;]
    S_Reg[&quot;레지스터&quot;]
    S_PC[&quot;PC&quot;]
  end

  subgraph Multi[&quot;멀티 스레드 프로세스&quot;]
    M_Code[&quot;코드 (공유)&quot;]
    M_Data[&quot;데이터 (공유)&quot;]
    M_Heap[&quot;힙 (공유)&quot;]
    subgraph Threads[&quot;스레드별 독립 영역&quot;]
      T1[&quot;스레드1
스택1 / 레지1 / PC1&quot;]
      T2[&quot;스레드2
스택2 / 레지2 / PC2&quot;]
      T3[&quot;스레드3
스택3 / 레지3 / PC3&quot;]
    end
  end

  style Single fill:#e8f4fd,stroke:#333
  style Multi fill:#e8fde8,stroke:#333
  style Threads fill:#fff3cd,stroke:#333
" />

### 프로세스 vs 스레드

| 항목 | 프로세스 | 스레드 |
|------|---------|--------|
| 메모리 | 독립적 (코드, 데이터, 힙, 스택 각각 소유) | 코드, 데이터, 힙 공유 / 스택, 레지스터는 독립 |
| 생성 비용 | 높음 (메모리 복사 필요) | 낮음 (공유 자원 활용) |
| 통신 | IPC 필요 (파이프, 소켓, 공유 메모리 등) | 공유 메모리로 직접 통신 (주의: 동기화 필요) |
| 컨텍스트 스위칭 | 비용 높음 (메모리 공간 전환) | 비용 낮음 (같은 메모리 공간) |
| 안정성 | 하나가 죽어도 다른 프로세스에 영향 없음 | 하나가 죽으면 전체 프로세스에 영향 |
| 비유 | 아파트의 서로 다른 가구 | 한 가구 내의 가족 구성원 |

### 멀티프로세스 vs 멀티스레드

| 비교 | 멀티프로세스 | 멀티스레드 |
|------|-----------|-----------|
| 구조 | 여러 프로세스가 협력 | 한 프로세스 내 여러 스레드 |
| 메모리 | 독립적 | 공유 |
| 안정성 | 높음 (격리됨) | 낮음 (하나의 오류가 전체에 영향) |
| 통신 비용 | 높음 (IPC) | 낮음 (공유 메모리) |
| 예시 | Chrome 브라우저 (탭마다 별도 프로세스) | Java/Python 웹 서버 (요청마다 스레드) |

> **실전**: Chrome은 각 탭을 별도 프로세스로 실행한다. 한 탭이 crash되어도 다른 탭에 영향이 없다. 반면 스레드 기반 서버는 메모리를 효율적으로 공유하지만, 하나의 버그가 전체 서버를 다운시킬 수 있다.
## 정리

| 항목 | 내용 |
|------|------|
| 프로세스 | 실행 중인 프로그램, 독립된 메모리 공간 |
| 메모리 구조 | 코드, 데이터, 힙, 스택 4영역 |
| 프로세스 상태 | New → Ready → Running → Waiting → Terminated |
| PCB | 프로세스 관리 정보 (PID, 상태, PC, 레지스터 등) |
| 컨텍스트 스위칭 | 현재 프로세스 상태 저장 → 새 프로세스 상태 복원 |
| 스레드 | 프로세스 내 실행 단위, 코드/데이터/힙 공유 |
| 프로세스 vs 스레드 | 프로세스: 독립/안정 / 스레드: 공유/경량 |
| Python GIL | Global Interpreter Lock — CPU 작업에서 멀티스레드 효과 제한 |
